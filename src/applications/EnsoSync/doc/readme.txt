EnsoSync

About
=====
EnsoSync is a file synchronization tool that works on two local structured directories.

Language
========
EnsoSync's structured domain description language is based on ???

EnsoSync also has a rule language that dictate when and how synchronization is performed. (TODO)

Tool
====
The tool is basically an interpreter that reads the desc language and runs the rules. You have to manually run (or crontab) it yourself

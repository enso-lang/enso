exp(x, 4) in
fun exp(x, n) =
    if (== n 0) then
      1
    else
      if (== (% n 2) 0) then
         square(exp(x, (/ n 2)))
      else
         (* x exp(x, (- n 1)))
      end
    end;
  fun square(x) = (* x x); 

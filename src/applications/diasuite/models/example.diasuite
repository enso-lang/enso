
device MotionDetector {
  source motion as Boolean;
}
 
device Screen {
  action Display;
}
 
action Display { 
  display(message as String); 
}
 
context Presence as Boolean {
  source motion from MotionDetector;
}
 
controller DisplayController {
  context Presence;
  action Display on Screen;
}
LHS type of render:

School (courses:Course*):
    puts "Earnings reports for Podunk High School"
    puts "---------------------------------------"
    courses.each {|c|Render(c)}

Course (cname:/str, duration:/int, cost:/int, students:Student*):
    puts "\nCourse: #{cname}"
    students.each {|s|Render(s)}
    puts "Cost to run = $#{duration*cost}"

Student (sname:/str, fees:/int):
    puts "  #{sname} ($#{fees})"

so

{courses:{cname:str, duration:int, cost:int, students:{sname:str, fees:int}*}*}